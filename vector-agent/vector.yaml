data_dir: /var/lib/vector

sources:
  app_logs:
    type: file
    include:
      - /logs/app.log
    read_from: beginning

sinks:
  console:
    type: console
    inputs:
      - app_logs
    encoding:
      codec: raw_message